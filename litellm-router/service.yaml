# Service for litellm
apiVersion: v1
kind: Service
metadata:
  name: litellm
  namespace: kserve-litellm-router
spec:
  selector:
    app: litellm
  ports:
  - port: 4000
    targetPort: 4000
    protocol: TCP