apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - litellm-config.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml

commonLabels:
  app: litellm-router
  managed-by: kserve