acmedns-account.yaml
kubespray-venv
qwen3coder/*.swp

# LiteLLM secrets — real master/salt keys must never be committed.
# Only litellm/secrets.example.yaml (a template) is tracked.
litellm/secrets.yaml
litellm/secret*.yaml
!litellm/secrets.example.yaml

# Testing scripts embed live API keys — keep them local.
curl.sh
